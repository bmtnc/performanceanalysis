% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calculate_performance_cone.R
\name{calculate_performance_cone}
\alias{calculate_performance_cone}
\title{Calculate performance cone}
\usage{
calculate_performance_cone(
  returns,
  sharpe_ratio,
  volatility,
  n_sigma = 3L,
  date_col = "date",
  return_col = "return"
)
}
\arguments{
\item{returns}{Data frame with date and return columns (simple returns)}

\item{sharpe_ratio}{Assumed Sharpe ratio (annualized, excess return / vol)}

\item{volatility}{Assumed annualized volatility (e.g. 0.15 for 15 percent)}

\item{n_sigma}{Number of standard deviation bands to compute (default 3)}

\item{date_col}{Name of the date column (default "date")}

\item{return_col}{Name of the return column (default "return")}
}
\value{
The input data frame with added columns: cumulative_return,
t_years, center, upper_1, lower_1, ..., upper_n, lower_n.
Cone values are cumulative returns (starting at 0). The result
carries attributes: sharpe_ratio, volatility, mu_arithmetic,
mu_geometric, variance_drain.
}
\description{
Enriches a returns data frame with a cone of expected performance paths
given a Sharpe ratio and volatility assumption. The center line is the
geometric growth rate (arithmetic return minus variance drain). Sigma
bands capture the lognormal dispersion of cumulative wealth over time.
}
